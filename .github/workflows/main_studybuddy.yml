name: Build and deploy StudyBuddy to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install backend dependencies
        working-directory: backend
        run: |
          uv sync --frozen

      - name: Prepare deployment package
        run: |
          rm -rf deploy
          mkdir -p deploy

          cp -r backend deploy/backend
          cp -r frontend deploy/frontend

          rm -rf deploy/backend/.venv
          rm -rf deploy/backend/__pycache__
          rm -rf deploy/frontend/node_modules

      - name: Upload deployment artifact
        uses: actions/upload-artifact@v4
        with:
          name: studybuddy-app
          path: deploy/

  deploy:
    runs-on: ubuntu-latest
    needs: build

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download deployment artifact
        uses: actions/download-artifact@v4
        with:
          name: studybuddy-app
          path: .

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0944281A8B894A36A64464B3E388C922 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_A1D3EE7C0F2F4AA893A28F7976961B50 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_075A6C388E9D48B5A35470510565BB34 }}
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: "studybuddy"
          slot-name: "Production"
