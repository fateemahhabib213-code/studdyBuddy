# Base URL of the StudyBuddy FastAPI backend (no trailing slash).
# The current backend does not use an /api prefix — adjust here only
# if you change the backend's route prefixes.
VITE_API_BASE_URL=http://127.0.0.1:8000
